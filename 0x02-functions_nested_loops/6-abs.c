#include "main.h"
/**
 * _abs - prints the absolute value
 *
 * @n: takes in basic integer value as an argument.
 *
 * Return: Always 0(success)
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
