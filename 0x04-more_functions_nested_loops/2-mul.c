#include "main.h"
/**
 * mul - calls the function mul
 *
 * @a: the first integer value
 * @b: the second integer value assigned to the function
 *
 * Return: the product of a and b (Success)
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
